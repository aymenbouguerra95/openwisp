apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: static-data
  name: static-data
  namespace: aymen
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
